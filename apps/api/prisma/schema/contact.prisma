model Contact {
  id           Int        @id @default(autoincrement())
  content      String
  email        String
  name         String
  enterpriseId Int
  enterprise   Enterprise @relation(fields: [enterpriseId], references: [id])
}
