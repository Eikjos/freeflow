model Media {
  id         Int         @id @default(autoincrement())
  extension  String
  uploadedPath   String
  entreprise Enterprise?
  expense    Expense?
  invoice    Invoice?
  project    Project?
  tasks      TaskMedia[]
  credits    Credit[]
}
