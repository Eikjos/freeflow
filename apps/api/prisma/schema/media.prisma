model Media {
  id           Int @id()@default(autoincrement())
  filename     String
  extension    String
  invoice      Invoice?
  entreprise   Enterprise?
  project      Project?
  expense      Expense?
}