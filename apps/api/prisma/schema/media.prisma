model Media {
  id         Int         @id @default(autoincrement())
  extension  String
  filename   String
  entreprise Enterprise?
  expense    Expense?
  invoice    Invoice?
  project    Project?
  tasks      TaskMedia[]
}
