model Task {
  id          Int     @id @default(autoincrement())
  name        String
  description String?
  index       Int
  columnId    Int
  column      Column  @relation(fields: [columnId], references: [id])
}
